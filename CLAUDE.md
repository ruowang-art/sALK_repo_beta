# Möuseley Kräs — Project Conventions (pointer)

The full, authoritative project conventions live at
[`Markdown_files/CLAUDE.md`](Markdown_files/CLAUDE.md), per this project's standing convention of
keeping every `.md` file in `Markdown_files/`. **This root file exists only so Claude Code reliably
auto-discovers project instructions** — read `Markdown_files/CLAUDE.md` in full before doing any
work in this repo; that file, not this one, is authoritative. Do not let this root file drift out
of sync with the real one — if the real conventions change, this pointer should stay exactly this
short and generic.
