# Möuseley Kräs

See [`Markdown_files/README.md`](Markdown_files/README.md) for the full project README (setup,
usage, dependency locks, safety behavior). This root file exists only so tooling that expects a
root-level `README.md` (GitHub's repo landing page, IDEs, etc.) finds something — all real
documentation for this project lives in `Markdown_files/`, per its standing convention.
